apiVersion: v1
kind: Service
metadata:
  name: set-api-service
spec:
  ## selector provided via kustomize
  #selector:
  #  app: set-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
